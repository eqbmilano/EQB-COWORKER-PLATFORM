
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "ROOT=$(git rev-parse --show-toplevel) && cd \"$ROOT/apps/web\" && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "apps/web/.next"
}
